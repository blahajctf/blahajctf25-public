deploy the contract, get the bytecode of the subcontract, notice that it performs some sort of comparison against a fixed string, win.

interesting element: decomp is partially broken due to another subcontract which is an obfuscated call to keccak